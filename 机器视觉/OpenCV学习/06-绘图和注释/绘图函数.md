# opencv绘图函数

## 绘制直线

```cpp
void cv::line(	
    InputOutputArray 	img,
    Point 	pt1,
    Point 	pt2,
    const Scalar & 	color,
    int 	thickness = 1,
    int 	lineType = LINE_8,
    int 	shift = 0 
)	
```

## 绘制多段线

```cpp
void cv::polylines	(	InputOutputArray 	img,
const Point *const * 	pts,
const int * 	npts,
int 	ncontours,
bool 	isClosed,
const Scalar & 	color,
int 	thickness = 1,
int 	lineType = LINE_8,
int 	shift = 0 
)	
```

## 绘制箭头

```cpp
void cv::arrowedLine(	
    InputOutputArray 	img,
    Point 	pt1,
    Point 	pt2,
    const Scalar & 	color,
    int 	thickness = 1,
    int 	line_type = 8,
    int 	shift = 0,
    double 	tipLength = 0.1 
)	
```

## 绘制三角形



## 绘制矩形

```cpp
void cv::rectangle	(	InputOutputArray 	img,
Point 	pt1,
Point 	pt2,
const Scalar & 	color,
int 	thickness = 1,
int 	lineType = LINE_8,
int 	shift = 0 
)	
```

## 绘制圆形

```cpp
void cv::circle(	
    InputOutputArray 	img,                // 绘图所在的图像cv::Mat
    Point 	            center,             // 圆心
    int 	            radius,             // 圆的半径
    const Scalar        &color,             // 颜色，标准的RGB
    int 	            thickness = 1,      // 线的粗细
    int 	            lineType = LINE_8,  // 线的类型：8连通或者4连通……
    int 	            shift = 0           // 缩放参数？
)	
```

## 绘制椭圆

## 判断直线是否在某个矩形范围内

```cpp

```

## 绘制轮廓

```cpp
void cv::drawContours	(	InputOutputArray 	image,
InputArrayOfArrays 	contours,
int 	contourIdx,
const Scalar & 	color,
int 	thickness = 1,
int 	lineType = LINE_8,
InputArray 	hierarchy = noArray(),
int 	maxLevel = INT_MAX,
Point 	offset = Point() 
)	
```

## 绘制标志

```cpp
void cv::drawMarker	(	InputOutputArray 	img,
Point 	position,
const Scalar & 	color,
int 	markerType = MARKER_CROSS,
int 	markerSize = 20,
int 	thickness = 1,
int 	line_type = 8 
)	
```
